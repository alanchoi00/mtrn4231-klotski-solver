sense:
  ros__parameters:
    # Board dimensions in cells
    board_width_cells: 4
    board_height_cells: 5

    min_cell_colour_area: 300 # Minimum area in pixels for a cell's color to be considered valid

    # Camera frame IDs
    camera_frame_id: "camera_color_optical_frame"
    camera_link_frame_id: "camera_link"

    # Board reference marker config
    #   TL ------ TR
    #    |        |
    #    |        |
    #   BL ------ BR
    #
    board_tl_marker_id: 0 # ID of the top-left corner marker on the board
    board_tr_marker_id: 1 # ID of the top-right corner marker on the board
    board_bl_marker_id: 2 # ID of the bottom-left corner marker on the board
    board_br_marker_id: 3 # ID of the bottom-right corner marker on the board
    marker_length_m: 0.065 # marker side length in meters

    # ArUco frame correction offsets (in base_link frame, meters)
    # Use these to correct systematic errors in depth-computed ArUco positions
    aruco_offset_x_m: 0.07526   # X offset correction (positive = further from robot)
    aruco_offset_y_m: 0.026514  # Y offset correction (positive = left of robot)
    aruco_offset_z_m: 0.0       # Z offset correction (positive = up)

    # Board offset from reference marker (in marker's local frame)
    board_offset_x_m: 0.040 # 40mm right (marker frame X)
    board_offset_y_m: 0.040 # 40mm up (marker frame Y)
    board_rotation_offset_deg: 90.0 # Rotation offset around Z-axis in degrees (adjust if gripper orientation is off)
    board_frame_id: "klotski_board" # board frame ID (offset from bottom-left marker)

    board_piece_counts:
      red: 1
      green: 1
      blue: 4
      yellow: 4
      empty: 2

    world_frame_id: "base_link"

    hsv_config_file: "hsv_ranges.default.yaml" # omitted path is relative to src/pkg_sense/config/
